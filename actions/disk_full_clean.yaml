---
name: "disk_full_clean"
pack: "certademo"
description: "handle when host out of disk space"
runner_type: "orquesta"
entry_point: "workflows/disk_full_clean.yaml"
enabled: true

parameters:
  instance:
    type: "string"
    description: "a hostip"
  mountpoint:
    type: "string"
    description: "mount point"