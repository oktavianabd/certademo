---
name: "drain_node"
pack: "certademo"
description: "handle drain node"
runner_type: "orquesta"
entry_point: "workflows/drain_node.yaml"
enabled: true

parameters:
  nodename:
    type: "string"
    description: "node name"
    required: true