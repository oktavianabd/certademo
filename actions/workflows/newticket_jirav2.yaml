version: 1.0
name: "newticket_jirav2"
description: "handle new ticket from jira"

tasks:
  notify:
    action: chatops.post_message
    input:
      channel: stackstorm-demo
      message: |
        There is new ticket from jira
        Ticket description: {{trigger.fields.status.description}}
    # next:
    #   - when: <% succeeded %>
    #     do: get_approval
